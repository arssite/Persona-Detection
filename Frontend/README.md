# Frontend (React + TypeScript)

## Setup

```bash
npm install
```

## Run

```bash
npm run dev
```

Configure the backend base URL via `.env` (Vite style):

```env
VITE_API_BASE_URL=http://localhost:8000
```
